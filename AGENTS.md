# Agent Instructions

- After any code or content changes, always `git commit` and `git push`.
- After pushing, always check the production deployment using the Vercel CLI (for example: `vercel deploy --prod` and confirm readiness with `vercel inspect`).
